# Model weights (*.safetensors) are NOT stored in git (they exceed GitHub's 100MB
# limit and LFS bandwidth is too small for a public model). They are hosted as a
# GitHub Release asset and downloaded by download_model.sh / entrypoint.sh.
